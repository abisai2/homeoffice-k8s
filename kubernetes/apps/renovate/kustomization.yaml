apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: renovate
resources:
  - cronjob.yaml
# GitHub fine-grained PAT (RENOVATE_TOKEN) — see secret.example.yaml.
generators:
  - secret-generator.yaml
