apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: cloudnative-pg
    repo: https://cloudnative-pg.github.io/charts
    releaseName: cnpg
    version: "0.28.2"          # chart 0.28.2 -> operator appVersion 1.29.1
    namespace: cnpg-system
    valuesFile: values.yaml
