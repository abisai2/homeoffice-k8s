# Minimal SA — the job never calls the Kubernetes API (token automount disabled on
# the pod). Exists only to avoid using the namespace `default` SA.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: etcd-backup
automountServiceAccountToken: false
