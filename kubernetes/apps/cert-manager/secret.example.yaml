# PLAINTEXT placeholder (no real values) — documents the Cloudflare token Secret shape.
# The real value lives ENCRYPTED in secret.sops.yaml. To set the real token:
#   task sops:edit -- kubernetes/apps/cert-manager/secret.sops.yaml
# Token scope (Cloudflare): Zone:DNS:Edit + Zone:Zone:Read on ap169homeoffice.net.
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
type: Opaque
stringData:
  api-token: REPLACE_WITH_REAL_CLOUDFLARE_TOKEN
