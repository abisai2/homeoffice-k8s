apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: databases
resources:
  - cluster.yaml
  - objectstore.yaml
  - scheduledbackup.yaml
  - database-authentik.yaml
  - database-netbox.yaml
  - database-litellm.yaml
# Wasabi S3 backup credential (databases ns) — see secret.example.yaml.
generators:
  - secret-generator.yaml
